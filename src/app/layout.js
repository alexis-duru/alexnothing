// This is a root layout that only exists to redirect to the localized version
// The actual layout logic is in [locale]/layout.js

export default function RootLayout({ children }) {
  return children;
}
